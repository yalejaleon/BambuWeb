<p id="notice"><%= notice %></p>

<h1>Tb Servicios</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tb_servicios.each do |tb_servicio| %>
      <tr>
        <td><%= link_to 'Show', tb_servicio %></td>
        <td><%= link_to 'Edit', edit_tb_servicio_path(tb_servicio) %></td>
        <td><%= link_to 'Destroy', tb_servicio, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tb Servicio', new_tb_servicio_path %>
